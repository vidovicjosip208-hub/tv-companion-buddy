# Kopiraj u android/keystore.properties i unesi svoje podatke.
# Ako datoteka postoji, `./gradlew assembleRelease` automatski potpisuje APK.
storeFile=cube-player.keystore
storePassword=CHANGE_ME
keyAlias=cube-player
keyPassword=CHANGE_ME
